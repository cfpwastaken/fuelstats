# Fuel Stats App

Environment variables for runtime:
- `DATABASE_URL` - URL for the database connection (`postgres://user:password@localhost:5432/fuel`)

1. `bun install` - Install dependencies
2. `bun run dev` - Start development server
3. `bun run build` - Build for production

You can also build a docker image: `docker build -t fuelstats .`.
